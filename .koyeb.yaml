version: "1.0"
apps:
  - name: osint-recon-bot
    services:
      - name: bot-worker
        type: worker
        builder: dockerfile
        instance_type: eco
        # Explicitly defining empty ports and disabling check vectors
        ports: []
        health_checks: []
        
